# Operations Runbook: Deploy Static Site

Static files in `dist/` are built locally and tracked in Git. Pushing to origin triggers Cloudflare Pages or GitHub Pages deployment depending on configured routing.
