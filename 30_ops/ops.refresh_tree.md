# Operations Runbook: Refresh Tree

The script automatically regenerates `dist/tree.html` during the build process, capturing the current structure of `C:\QiLabs` while ignoring standard test/venv folders.
